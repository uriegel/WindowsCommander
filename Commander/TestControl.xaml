<UserControl x:Class="Commander.TestControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"     
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Commander"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" 
             SizeChanged="UserControl_SizeChanged">
    <UserControl.Resources>
        <Storyboard x:Key="WaterRipples" >
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Effect).(local:WaterRipple.Phase)" 
                                           Storyboard.TargetName="PeopleListView">
                <EasingDoubleKeyFrame KeyTime="0" Value="20"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.3" Value="10"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.6" Value="0"/>
            </DoubleAnimationUsingKeyFrames>
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Effect).(local:WaterRipple.Amplitude)" 
                                           Storyboard.TargetName="PeopleListView">
                <EasingDoubleKeyFrame KeyTime="0" Value="0"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.3" Value="4"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.6" Value="0"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBox KeyboardNavigation.IsTabStop="False"
                 KeyDown="TextBox_KeyDown" GotFocus="TextBox_GotFocus" BorderThickness="0" />
        <local:ColumnViewHeader Grid.Row="1" x:Name="griddie" />
        <ListView Grid.Row="2" x:Name="PeopleListView" 
                  VirtualizingPanel.VirtualizationMode="Recycling" 
                  HorizontalContentAlignment="Stretch"
                     SelectionMode="Multiple"
                  PreviewMouseDown="PeopleListView_PreviewMouseDown"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled" SelectionChanged="PeopleListView_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ItemsControl}, 
                                Path=DataContext.W1}" />
                            <ColumnDefinition Width="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ItemsControl}, 
                                Path=DataContext.W2}" />
                            <ColumnDefinition Width="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ItemsControl}, 
                                Path=DataContext.W3}" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Margin="0, 0, 5, 0" Text="{Binding Name}" Padding="0" TextTrimming="CharacterEllipsis" />
                        <TextBlock Margin="0, 0, 5, 0" Grid.Column="1" TextTrimming="CharacterEllipsis" Text="{Binding DateTime}" />
                        <TextBlock Margin="5, 0, 0, 0" Grid.Column="2" TextAlignment="Right" TextTrimming="CharacterEllipsis"
                            Text="{Binding Size}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>

            <ListView.Resources>
                <Style TargetType="ListViewItem" BasedOn="{StaticResource {x:Type ListViewItem}}">
                    <Setter Property="Padding" Value="4,2"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="MinHeight" Value="0"/>
                </Style>
            </ListView.Resources>
        </ListView>
    </Grid>
</UserControl>
